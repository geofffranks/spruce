---
meta:
  numA: 8.333
  numB: 3.666

properties:
  homework:
    max: (( calc "max(meta.numA, meta.numB)" ))
    min: (( calc "min(meta.numA, meta.numB)" ))
    mod: (( calc "mod(meta.numA, meta.numB)" ))
    pow: (( calc "pow(meta.numA, meta.numB)" ))
    sqrt: (( calc "sqrt(meta.numA)" ))
    floor: (( calc "floor(meta.numB)" ))
    ceil: (( calc "ceil(meta.numA)" ))
